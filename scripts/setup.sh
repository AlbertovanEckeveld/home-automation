#!/usr/bin/env bash
# This setup script has been deprecated and removed from the project.
# The repository now includes docker-compose and other automation scripts.
# If you need system-level setup for a fresh host, recreate a new script
# or use the recommended Docker-based deployment in docker-compose.yml.

echo "This setup.sh has been removed. Use docker-compose or scripts/gen_go2rtc_config.py instead."
exit 0
